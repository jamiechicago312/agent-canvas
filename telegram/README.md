# OpenHands Telegram Bridge

Local Telegram integration service for Agent Canvas development.

This package is launched automatically by `npm run dev` and stays idle until
Telegram is enabled from **Settings → Integrations → Telegram**.
